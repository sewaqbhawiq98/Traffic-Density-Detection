specVersion: "v1"
serviceId: "traffic-density-system"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "traffic-density-system-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
